### Override local settings in `.mise.local.toml` which is untracked by git

[tools]
bun = '1'
rust = '1'
node = 'lts'

[settings]
status.show_env = true
status.show_tools = true

[env]
# _.file = '.env.local'
# DEBUG = 'eslint:eslint'
