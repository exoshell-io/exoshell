{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["scripts/**"],
  "pipeline": {
    "build:": {
      "dependsOn": ["^build:"],
      "outputs": ["dist/**"]
    },
    "clean": { "cache": false },
    "lint": {},
    "test": {}
  }
}
